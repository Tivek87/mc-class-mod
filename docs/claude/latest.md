# Laatste sessie — 2026-09-26 (ochtend)

- **Vraag:** flamethrower groter + mooiere vlammen, vloeiende overgangen, linksklik breder; daarna: 35% kleiner, alleen links↔rechts vegen (12 aanvallen weg), hoofd niet scheef; 5 spells echt upgraden (visueel), eigen bliksem-VFX.
- **Flamethrower:** gun 1,3x origineel (`GUN_SCALE` 1.2, `OWN_GUN` 0.91), voorgreep iets naar achter. Alleen SWEEP/SWEEP_BACK (±82,5°, `ACROSS` 15°), om en om, vlak (geen op/neer). Eigen lichaamsmodel `FlameBody` (heup-scharnier, knieën via `KneelLegs`, zakken), `FlameAttacks` (aanloop + doorzwaai), levendige idle-stance. Vlammen: kolkende lobben, plasma-kern, vonken, rook, spetters, mondingsring; vervagen bij de camera. Hoofd kantelt niet meer.
- **Spells:** nieuw `SpellFxPayload` + `spell/client/SpellFx` (FireFx, StormFx, PoisonFx, WindFx, VoidFx, Particles). Gameplay: fireball gebiedsschade + dooft in water; bliksem zonder vanilla-bolt (`thunderHit`), shock + ketting 3 (5 bij regen); poison vertraagt + sterker na 2 s; wind kaatst schoten terug, items weg, zachte landing; void hinderlaag +50% blind/traag.
- **Getest in-game (6 runs):** schok-meting animatie (geen sprongen), screenshots flamethrower (voor/eerste persoon) en alle spells; schade/effecten gelogd (ketting, hinderlaag 4→5,9, poison traag). `gradlew build` OK (0.2.1-alpha). Testklasse, wereldkopie en screenshots verwijderd.
- **Docs:** POWERS.md, GREEN_LANTERN.md, SPELLS.md, PROJECT.md, CLAUDE.md (code-indeling), en_us, CHANGELOG 0.2.1-alpha; `release.ps1 prepare` → 0.2.1-alpha.
- **Niet getest:** multiplayer (payload alleen in singleplayer); void-effect zoals anderen het zien (eigen scherm is void-beeld).
- **Daarna:** bliksemgenerator en grotere wolk gebouwd, op verzoek allebei weer teruggedraaid: bliksem is weer de eerste eigen VFX (voorloper + 3 inslagen, kleine wolk op 18 hoog).
- **Commit `e51d146` gepusht** (publish afgebroken door gebruiker). Daarna: tijdens vuren gleden de benen bij lopen → stand vervaagt nu met de loopsnelheid (`FlameSeen.standing`), benen lopen normaal; getest in-game (lopen + vegen, zijaanzicht).
- **Release:** fix gecommit + gepusht, `v0.2.1-alpha` gepubliceerd (bevat alles van deze sessie).
- **Open:** `docs/reference/nanotech-ironman-skin.png` staat nog untracked (niet van deze sessie). `docs/reference/nanotech-ironman-skin.png` staat nog untracked (niet van deze sessie). Pose-kanalen kneel/hop/orbit bestaan nog maar worden nu niet gebruikt.
